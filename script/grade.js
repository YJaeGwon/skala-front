// 3과목 성적 계산기
function checkGrade() {
  var subjects = ["HTML", "CSS", "JavaScript"];
  var total = 0;

  // 1) subjects 배열 길이만큼 반복하며 prompt()로 점수를 입력받아 total에 합산
  //    예: prompt(subjects[i] + " 점수를 입력하세요.");

  // 2) 평균 계산 후 60점 이상이면 합격, 미만이면 불합격 판정

  // 3) alert("총점: ..., 평균: ..., 결과: ...") 형태로 출력
}
