// Open-Meteo API 호출 담당 모듈
export async function fetchWeather(lat, lon) {
  // fetch()와 async/await로 Open-Meteo API 호출
  // 예: https://api.open-meteo.com/v1/forecast?latitude=위도&longitude=경도&current_weather=true

  // 응답에서 필요한 온도/습도 등의 데이터를 꺼내 반환
}
